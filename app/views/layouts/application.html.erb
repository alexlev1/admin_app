<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <title>Административная панель</title>
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://fonts.googleapis.com/css?family=Roboto:400,500,700&amp;subset=cyrillic,cyrillic-ext" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="top-menu">
      <div class="container">
        <div class="d-flex flex-column flex-md-row align-items-center pt-3 pb-3 mb-3 bg-white box-shadow">
          <%= link_to root_path, class: 'root-link my-0 mr-md-auto' do %>
            <div class="logo">Админка учета пользователей</div>
          <% end %>

          <% if admin_signed_in? %>
            <nav class="my-2 my-md-0">
              <%= link_to 'Личный кабинет', users_path, class: 'btn btn-outline-primary' %>
              <%= link_to 'Выйти', destroy_admin_session_path, method: :delete, class: 'p-2 text-blue' %>
            </nav>
          <% else %>
            <nav class="my-2 my-md-0">
              <%= link_to 'Войти', new_admin_session_path, class: 'btn btn-outline-primary' %>
            </nav>
          <% end %>
        </div>

        <%= bootstrap_flash %>

      </div>
    </div>

    <div class="container">

      <%= yield %>

    <div>

    <div class="footer">
      <div class="container">
        <div class="pt-3 mt-3 mb-4 border-top">
          <div class="row">
            &copy; "Админка", 2019 - ∞
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
